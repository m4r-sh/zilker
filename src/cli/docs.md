# init flow
- Ask for folder names (list)
- Test list for common names (pages, views, ui, assets, static, etc)
- Assign each folder a specific file model
- Each model has scaffolding guides, error tips, and metrics
- (Done)
- Write zilker.js to encode answers to init quiz
- Suggest zilk dev (which uses zilker.js to define a build system and folder watching system)

